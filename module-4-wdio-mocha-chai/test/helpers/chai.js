import { assert, expect as chaiExpect, should } from 'chai';


// Chai provides three assertion styles: assert, should, and expect
// The should interface must be initialized once before it can be used in tests
should();

export { assert, chaiExpect };